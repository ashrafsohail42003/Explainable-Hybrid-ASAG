# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/

# Virtual envs
.venv/
venv/
env/
.python-version

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/
.cache/

# Data — never commit raw or processed datasets (keep folders via .gitkeep)
data/raw/*
data/interim/*
data/processed/*
data/external/*
!data/raw/.gitkeep
!data/interim/.gitkeep
!data/processed/.gitkeep
!data/external/.gitkeep
*.zip
*.tar.gz
*.parquet
*.jsonl
# Note: do NOT blanket-ignore *.csv — Mohler raw CSV stays in data/raw which is already covered above

# Reports figures: keep PNGs in repo for the writeup
!reports/figures/*.png

# Jupyter outputs
.ipynb_checkpoints/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude / agents
.claude/

# Kaggle credentials — never commit
kaggle.json

CLAUDE.md

# Excluded from the public repo (Arabic narrative report + scratch)
reports/*.html
reports/*.pdf
_demo_*
_smoke*

# --- Publication cleanup: exclude scratch / internal / build artifacts ---
_demo_neural.py
_demo_neural.log
_smoke_neural.py
_smoke.log
run_runpod.sh
run_runpod_v2.sh
CLAUDE.md
README.md.bak
Explainable-Hybrid-ASAG*.zip
results_bundle*.zip
# LaTeX build artifacts (keep .tex, .bbl, .pdf)
paper/*.aux
paper/*.blg
paper/*.out
paper/*.log
